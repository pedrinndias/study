//Declare duas variáveis inteiras, a e b. Atribua valores a elas. Declare uma terceira variável, soma, e 
//guarde nela o resultado da soma de a e b. Imprima o resultado.

#include<stdio.h>
int main(){
    int a,b;
    a=4;
    b=5;
    int soma;
    soma=a+b;
    printf("A soma da variavel a + b eh: %d", soma);
    return 0;
}